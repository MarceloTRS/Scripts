# Scripts
# Scripts Shell Linux lista Marcelo

- backup-amazon.sh                         = backup e restauração de um banco de dados, backup hospedado em um bucket S3 na Amazom
- backup-dados-mysql.sh                    = dump do banco do dados mysql
- comandos.sh                              = subir lista de comandos
- conversao-jpg-png.sh                     = conversao de extensão foros jpg>png
- filtrando-resultado-apache.sh            = filtrando resultado do logs do Apache
- filtro-requisicao.sh                     = filtrar requisicao dos logs do Apache
- monitoracao-servidor-mensagem-tela.sh    = monitora serviço do Apache e em caso de problemas reinicia o mesmo
- monitoracao-servidor.sh                  = monitora serviço do Apache e em caso de problemas reinicia o mesmo e envia um email de alerta
- restore-amazon.sh                        = download e restore de uma banco de dados hospedado na Amazon S3
- verificacao-memoria-consumida.sh         = verifica o consumo de memoria e envia um email de alerta


########################
